# image_classification
using the CNN (Convolutional neural network to clasify the image in this i am use the "CIFAR10" data set which is contain this this images ['plane','car','bird','cat','deer','dog','frog','horse','ship','truck']
